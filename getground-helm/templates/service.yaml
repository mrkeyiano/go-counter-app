apiVersion: v1
kind: Service
metadata:
  name: {{ include "getground-helm.fullname" . }}
  labels:
    {{- include "getground-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "getground-helm.selectorLabels" . | nindent 4 }}
